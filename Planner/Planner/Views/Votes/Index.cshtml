@model IEnumerable<Planner.Models.Vote>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_GeneralLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OpenAnswer)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OpenAnswer)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VoteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VoteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VoteId })
        </td>
    </tr>
}

</table>
